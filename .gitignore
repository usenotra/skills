# Skill eval run workspaces (regenerable scratch: run outputs, benchmarks,
# review HTML, description-optimization logs). The eval *definitions* live in
# each skill's own evals/ folder and are committed.
*-workspace/

# Python bytecode cache
__pycache__/
*.pyc
